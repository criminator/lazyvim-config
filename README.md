# Neovim config
- I use Wezterm with pwsh.exe (Powershell 7), so if you use that then this configuration should work for you.
- This config also expects MinGW, if you use something else then change `--query-driver` in [example.lua](lua/plugins/example.lua)
